[package]
name = "password_generator"
version = "0.2.0"
edition = "2021"

[dependencies]
rand = "0.8"
clap = { version = "4.2", features = ["derive"] }
arboard = "3.2"   # для копіювання в буфер обміну
